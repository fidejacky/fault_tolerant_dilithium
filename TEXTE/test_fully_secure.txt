wir haben zwar den testbench, aber es zeigt nicht ob :
1. alle FF wirklich geschutzt sind 
2. der schutz nicht wegoptimiert worden ist 
3. der Voter wirklich bei fault hilft 
 aber ich haber schon die einzelne FF getstet, also von daher wenn Bedingung 2. erfüllt wäre, 
wäre dann auch die 3. Bedingung erfüllt) 
=> ein struktureller Check muss durchgeführt werden 

# Wie viele trm-FFs?
puts "trm_fdre: [llength [get_cells -hier -filter {REF_NAME == trm_fdre}]]"
puts "trm_fdse: [llength [get_cells -hier -filter {REF_NAME == trm_fdse}]]"

# Sind noch ungeschützte FF-Primitives übrig?
puts "FDRE left: [llength [get_cells -hier -filter {REF_NAME == FDRE}]]"
puts "FDSE left: [llength [get_cells -hier -filter {REF_NAME == FDSE}]]"
puts "FDCE left: [llength [get_cells -hier -filter {REF_NAME == FDCE}]]"
puts "FDPE left: [llength [get_cells -hier -filter {REF_NAME == FDPE}]]"


# BEOBACHTUNG BEIM TESTEN 
der Testverfahren läuft sehr lang: 
* too many words wird immer gesagt, vielleicht soll diese Variable TV erhöht werden, aber ich habe aktuell schon 20 
auf dem REpo steht sogar nur 5
aber am Endeffekt ist das nur wichtig um zu schauen aber ändert ja nichts an dem Design 

# Fragen: 
wie kann ich der Fault Toleranz in den gesamten Design beweisen ? => mit dem 
simulator
oder reicht es da dass die einzelene Bauteile getestet worden sind? also die isolierten tests?  
wie viele soll NUM_TV sein ? oder ist das egal ? 

## bei der ANalys:
reicht es dass ich die outputs und benchmarks vergleiche? 

#brouillon 
WARNING: [Wavedata 42-489] Can't add object "/tb_sign_top/z_2" to the wave window because it has 368640 bits, which exceeds the display limit of 65536 bits.  
To change the display limit, use the command "set_property display_limit <new limit> [current_wave_config]".
